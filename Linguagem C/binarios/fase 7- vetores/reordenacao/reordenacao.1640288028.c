#include<stdio.h>
main() {
	int v[5]= {56,54,87,25,21},vt[5],atual=0,x=0;
	//executei o ciclo de escaneamento para saber qual variavel é menor
	do{
		if(v[x]>atual)
		atual=v[x];
		x++;
	}while(x<10);
	//defino o valor da posição do meu vetor de transferencia
	vt[y]=menor
	}
